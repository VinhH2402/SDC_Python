CREATE TABLE review (
    id INTEGER,
    product_id INTEGER,
    rating INTEGER,
    date TIMESTAMP,
    summary TEXT, 
    body TEXT,
    recommend INTEGER,
    reported INTEGER,
    response TEXT,
    helpfulness INTEGER,
    reviewer INTEGER
);
